spring:
  datasource:
    url: ${CATEGORY_DB_URL}
    username: ${CATEGORY_DB_USER}
    password: ${CATEGORY_DB_PASS}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
