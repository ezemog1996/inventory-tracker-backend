spring:
  datasource:
    url: ${ITEM_DB_URL}
    username: ${ITEM_DB_USER}
    password: ${ITEM_DB_PASS}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
